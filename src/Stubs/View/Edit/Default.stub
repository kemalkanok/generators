@extends('app')

@section('title')
{{trans('all.{{model}}.new')}}
@stop

@section('content')
<form action="{{route('{{model}}.store')}}" method="post">
    <input type="hidden" name="_token" value="{{csrf_token()}}"/>
    <input type="hidden" name="_method" value="PUT"/>
    {{form}}
  <button type="submit" class="btn btn-default">{{trans('all.update')}}</button>
</form>
@stop
